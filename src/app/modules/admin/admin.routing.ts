import { Routes } from '@angular/router';
import { ResaleFormComponent } from './components/resale-form/resale-form.component';

export const AdminRoutes: Routes = [
    {
        path: '',
        component: ResaleFormComponent,
    },
];
